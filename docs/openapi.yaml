openapi: 3.0.3
info:
  title: Search Engine API
  version: "1.0"
  description: Minimal OpenAPI spec (placeholder)
servers:
  - url: http://localhost:8080
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  postgres:
                    type: boolean
                  redis:
                    type: boolean
                  timestamp:
                    type: string
                    format: date-time


