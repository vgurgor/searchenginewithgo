# golangci-lint v1.64+ Migration Kılavuzu

## 🔄 Yapılan Değişiklikler

### Deprecated Linter'ların Değişimi

#### 1. `golint` → `revive`
**Sebep:** golint artık geliştirilmiyor ve deprecated.

**Önceki:**
```yaml
linters:
  enable:
    - golint
```

**Yeni:**
```yaml
linters:
  enable:
    - revive

linters-settings:
  revive:
    confidence: 0.8
```

#### 2. `gomnd` → `mnd`
**Sebep:** Linter yeniden isimlendirildi (magic number detector).

**Önceki:**
```yaml
linters:
  enable:
    - gomnd

linters-settings:
  gomnd:
    settings:
      mnd:
        checks: argument,case,condition,return
```

**Yeni:**
```yaml
linters:
  enable:
    - mnd

linters-settings:
  mnd:
    checks:
      - argument
      - case
      - condition
      - return
```

#### 3. `maligned` → Kaldırıldı
**Sebep:** Deprecated, benzer işlevsellik `govet` içinde `fieldalignment` olarak mevcut.

**Alternatif:**
```yaml
linters:
  enable:
    - govet
```

### Konfigürasyon Yapısı Değişiklikleri

#### 4. `govet.check-shadowing` → `govet.enable`
**Önceki:**
```yaml
linters-settings:
  govet:
    check-shadowing: true
```

**Yeni:**
```yaml
linters-settings:
  govet:
    enable:
      - shadow
```

#### 5. `nolintlint.allow-leading-space` → Kaldırıldı
**Sebep:** Artık desteklenmiyor.

**Önceki:**
```yaml
linters-settings:
  nolintlint:
    allow-leading-space: true
    allow-unused: false
```

**Yeni:**
```yaml
linters-settings:
  nolintlint:
    allow-unused: false
    require-explanation: false
    require-specific: false
```

#### 6. `run.skip-dirs` ve `run.skip-files` → `issues.exclude-dirs` ve `issues.exclude-files`
**Sebep:** Daha mantıklı ve tutarlı yapı.

**Önceki:**
```yaml
run:
  skip-dirs:
    - vendor
    - tmp
  skip-files:
    - ".*_generated.go$"
```

**Yeni:**
```yaml
issues:
  exclude-dirs:
    - vendor
    - tmp
  exclude-files:
    - ".*_generated\\.go$"
```

## 🔍 Konfigürasyon Doğrulama

Konfigürasyonunuzu test etmek için:

```bash
cd backend
golangci-lint config verify
```

Başarılı çıktı:
```
configuration is valid
```

## 📚 Referanslar

- [golangci-lint v1.64 Release Notes](https://github.com/golangci/golangci-lint/releases/tag/v1.64.0)
- [golangci-lint Configuration Documentation](https://golangci-lint.run/usage/configuration/)
- [Deprecated Linters List](https://golangci-lint.run/usage/linters/)

## ✅ Kontrol Listesi

- [x] `golint` → `revive` değişimi
- [x] `gomnd` → `mnd` değişimi
- [x] `maligned` kaldırıldı
- [x] `govet.check-shadowing` → `govet.enable: [shadow]`
- [x] `nolintlint.allow-leading-space` kaldırıldı
- [x] `run.skip-*` → `issues.exclude-*` taşındı
- [x] Konfigürasyon validation başarılı
- [x] CI/CD pipeline güncellendi (v3 → v6)

## 🚀 Sonuç

Tüm deprecated ayarlar kaldırıldı ve golangci-lint v1.64+ ile uyumlu hale getirildi. Artık CI/CD pipeline'ında herhangi bir validation hatası almayacaksınız.

